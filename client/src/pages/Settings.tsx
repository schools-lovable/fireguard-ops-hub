/** Settings is a contextual route that opens the existing profile workspace directly on the requested section. */
import Profile from "./Profile";

export default function Settings() {
  return <Profile />;
}
